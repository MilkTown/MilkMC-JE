#WorldDownLoader detection
WorldDownLoader:

  #Do you want to open this detection?
  enable: true
  
  #If watchcat detects that the player is using the cheating, what command is executed?
  actions:
  - "kick {player} (WORLDDOWNLOADER)"
  
#LabyMOD detection
LabyMOD:

  #Do you want to open this detection?
  enable: true
  
  #If watchcat detects that the player is using the cheating, what command is executed?
  actions:
  - "kick {player} (LABYMOD)"